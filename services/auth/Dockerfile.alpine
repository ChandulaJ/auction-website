FROM node:18-alpine

WORKDIR /app

# Install git (needed for npm install from git)
RUN apk add --no-cache git

# Copy package.json and install dependencies
COPY package.json ./
RUN npm install

# Copy the rest of the application
COPY . .

# Expose the port
EXPOSE 3101

# Start the application
CMD ["npm", "start"]
